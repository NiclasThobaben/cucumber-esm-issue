import {Then} from '@cucumber/cucumber';
import {TestWorld} from "../worlds/WorldConfig";


Then(
    /^Error \[ERR_REQUIRE_ESM\] is thrown$/,
    function (this: TestWorld, string: string) {

    }
)